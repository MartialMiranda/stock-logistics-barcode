<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_stock_barcodes_read_inventory_form" model="ir.ui.view">
        <field name="name">stock.barcodes.read.inventory.form</field>
        <field name="model">wiz.stock.barcodes.read.inventory</field>
        <field name="inherit_id" ref="stock_barcodes.view_stock_barcodes_read_form" />
        <field name="mode">primary</field>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <field name="product_qty" position="after">
                <field name="inventory_product_qty" invisible="1" />
            </field>
            <!-- hide result package from inventory -->
            <group name="quant_package" position="replace">
                <div class="mt4">
                    <strong class="d-none d-sm-block">Package</strong>
                    <span class="fa fa-inbox d-sm-none oe_span_small_icon" title="package" />
                    <field name="package_id" options="{'no_create': True}" readonly="not manual_entry" force_save="1" style="width:85%" class="h5" />
                </div>
            </group>
            <field name="location_id" position="attributes">
                <attribute name="domain">[('usage', 'in', ['internal', 'transit'])]</attribute>
            </field>
            <group name="scan_fields" position="after">
                <group string="Inventory quants" invisible="inventory_quant_ids == []" col="2">
                    <field name="inventory_quant_ids" options="{'no_open': True}" nolabel="1" colspan="2" mode="tree,kanban">
                        <tree>
                            <field name="id" invisible="1" />
                            <field name="product_id" options="{'no_open': True}" optional="show" />
                            <field name="location_id" options="{'no_open': True}" groups="stock.group_stock_multi_locations" optional="show" />
                            <field name="lot_id" groups="stock.group_production_lot" options="{'no_open': True}" optional="show" />
                            <field name="package_id" groups="stock.group_tracking_lot" options="{'no_open': True}" optional="show" />
                            <field name="inventory_quantity" />
                            <field name="product_uom_id" groups="uom.group_uom" options="{'no_open': True}" />
                            <button name="action_barcode_inventory_quant_edit" type="object" icon="fa-pencil" class="btn mt0" context="{'wiz_barcode_id': parent.id}">
                            </button>
                            <button name="action_barcode_inventory_quant_unlink" type="object" class="btn" icon="fa-trash" title="Remove quant" context="{'wiz_barcode_id': parent.id}" />
                        </tree>
                        <kanban class="o_kanban_mobile">
                            <field name="id" />
                            <field name="product_id" />
                            <field name="location_id" />
                            <field name="lot_id" />
                            <field name="package_id" />
                            <field name="inventory_quantity" />
                            <field name="product_uom_id" />
                            <templates>
                                <t t-name="kanban-box">
                                    <div t-attf-class="oe_kanban_card">
                                        <div class="row">
                                            <div class="col-7">
                                                <t t-if="record.product_id and record.product_id.value">
                                                    <strong>
                                                        <span t-esc="record.product_id.value"/>
                                                    </strong>
                                                </t>
                                                <t t-if="!record.product_id or !record.product_id.value">
                                                    <strong>
                                                        <span>Producto no definido</span>
                                                    </strong>
                                                </t>
                                            </div>
                                            <div class="col-2">
                                                <span class="float-end text-end fw-bold">
                                                    <span t-if="record.inventory_quantity and record.inventory_quantity.value" t-esc="record.inventory_quantity.value"/>
                                                    <span t-if="!record.inventory_quantity or !record.inventory_quantity.value">0</span>
                                                </span>
                                            </div>
                                            <div class="col-3 text-end">
                                                <button name="action_barcode_inventory_quant_edit" type="object" class="btn mt0" context="{'wiz_barcode_id': parent.id}">
                                                    <i class="fa fa-pencil" title="Edit inventory quantity" style="font-size:1.5em" />
                                                </button>
                                                <button name="action_barcode_inventory_quant_unlink" type="object" string="Reset" icon="fa-trash" title="Reset inventory quantity" class="btn mt0" context="{'wiz_barcode_id': parent.id}" />
                                            </div>
                                        </div>
                                        <div class="row">
                                            <div class="col-4">
                                                <t t-if="record.location_id and record.location_id.value">
                                                    Loc: <span t-esc="record.location_id.value"/>
                                                </t>
                                                <t t-if="!record.location_id or !record.location_id.value">
                                                    Loc: <span>Ubicación no definida</span>
                                                </t>
                                            </div>
                                            <div class="col-4">
                                                <t t-if="record.lot_id and record.lot_id.value">
                                                    Lot: <span t-esc="record.lot_id.value"/>
                                                </t>
                                                <t t-if="!record.lot_id or !record.lot_id.value">
                                                    Lot: <span>Lote no definido</span>
                                                </t>
                                            </div>
                                            <div class="col-4">
                                                <t t-if="record.package_id and record.package_id.value">
                                                    Package: <span t-esc="record.package_id.value"/>
                                                </t>
                                                <t t-if="!record.package_id or !record.package_id.value">
                                                    Package: <span>Paquete no definido</span>
                                                </t>
                                            </div>
                                        </div>
                                    </div>
                                </t>
                            </templates>
                        </kanban>
                    </field>
                </group>
            </group>
            <div name="manual_entry" position="after">
                <div class="d-flex" name="display_read_quant">
                    <field name="display_read_quant" widget="barcode_boolean_toggle" id="display_read_quant" />
                    <label for="display_read_quant" />
                </div>
            </div>
            <button name="action_clean_values" position="before">
                <button name="apply_inventory" type="object" class="btn-primary ms-auto btn-sm" invisible="display_menu == True or inventory_quant_ids == []" data-hotkey="7" groups="stock.group_stock_manager">
                    <span>Apply</span>
                </button>
            </button>
        </field>
    </record>
    <!--
     Open wizard in current target option to avoid that the wizard is
     closed after any button click,
    -->
    <record id="action_stock_barcodes_read_inventory" model="ir.actions.act_window">
        <field name="res_model">wiz.stock.barcodes.read.inventory</field>
        <field name="name">Barcodes Read</field>
        <field name="view_mode">form</field>
        <field name="context">{"control_panel_hidden": True,
                               "form_view_initial_mode": "edit",
                               "inventory_mode": True,
                               }
        </field>
        <field name="view_id" ref="view_stock_barcodes_read_inventory_form" />
        <field name="target">current</field>
    </record>
</odoo>
